# Opcional. Defina apenas se o backend estiver hospedado em um domínio
# diferente do frontend. Se front e back ficarem no mesmo domínio
# (atrás de um reverse proxy, por exemplo), deixe sem definir.
VITE_API_URL=https://api.seudominio.com/api
